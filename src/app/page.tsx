import ImageProcessor from '@/components/ImageProcessor'; // Adjust path if needed

export default function HomePage() {
  return (
    <main className="min-h-screen bg-base-300 py-10">
      <ImageProcessor />
    </main>
  );
}